'use babel';

import ClaimspaceCloudView from '../lib/claimspace-atom-ide-view';

describe('ClaimspaceCloudView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
